name: web_frontend
description: Frontend that is accessed via a web browser
version: 1.0.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  bloc: ^8.1.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  flutter_gen: any
  flutter_localizations:
    sdk: flutter
  http: ^1.2.1
  intl: ^0.18.1
  middleware_api_utilities: ^0.1.0+1
  provider: ^6.1.2
  shared_data_models: ^0.1.0+1
  shared_preferences: ^2.2.3
  shared_widgets: ^0.1.0+1
  zoom_widget: ^2.0.1

dev_dependencies:
  bloc_test: ^9.1.6
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/6OG_bp.png
