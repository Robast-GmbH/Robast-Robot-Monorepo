// ignore_for_file: prefer_const_constructors

import 'package:flutter_test/flutter_test.dart';
import 'package:shared_widgets/shared_widgets.dart';

void main() {
  group('SharedWidgets', () {
    test('can be instantiated', () {
      expect(SharedWidgets(), isNotNull);
    });
  });
}
