This project contains code for door detection and finding nav points in a feuerplan.

Example usage:
    Make sure to install required libraries.

    cd src/preprocessing/py/door_detection_in_feuerplan
    python3 main.py -i FRP_Musterplan.svg.png -m door_detector_weights.pt